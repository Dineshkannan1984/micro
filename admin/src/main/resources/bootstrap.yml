spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - name: admin
          - name: app
      secrets:
        enable-api: true
        enabled: true
        sources:
          - name: appsecret
      reload:
        enabled: false